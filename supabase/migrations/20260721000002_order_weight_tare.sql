-- Редактируемая тара (вес пустого контейнера) на заявке.
-- Раньше тара жёстко бралась из словаря CONTAINER_TARE_WEIGHT по типу контейнера
-- и в БД не хранилась. Для рефконтейнеров реальная тара плавает на несколько
-- центнеров в зависимости от холодильной установки, поэтому клиент теперь может
-- задать/скорректировать её. NULL → как раньше, значение из словаря по типу.

ALTER TABLE orders ADD COLUMN IF NOT EXISTS weight_tare INTEGER;
